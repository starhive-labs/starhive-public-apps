// The Mantine Rating already matches Starhive's RATE attribute look (it's what platform-ui uses), so
// the SDK re-exports it directly. Use `readOnly` for display and `fractions={2}` for half-stars.
// eslint-disable-next-line no-restricted-imports
export { Rating, type RatingProps } from '@mantine/core'
